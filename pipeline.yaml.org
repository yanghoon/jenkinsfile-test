type: container

build:
- maven: mvn --version
- docker: docker version

deploy:
- k8s: k8s/*.yaml

agent:
  volumes:
  - persistentVolumeClaim:
      claimName: slave-workspace
      mountPath: /app/workspace

stage:
- build
- deploy
